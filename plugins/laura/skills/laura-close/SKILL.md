---
name: laura-close
description: Close the file panel in the current Laura tab. Use when running inside a Laura tab (LAURA_TAB is set) and you're done showing a file.
when_to_use: You are running inside a Laura tab (LAURA_TAB is set) and want to close the open side panel.
user-invocable: true
---

# laura close

Close the current tab's side panel.

```bash
laura close
```
